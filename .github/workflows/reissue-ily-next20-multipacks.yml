name: Reissue ILY next 20 multipacks
on:
  push:
    branches: [agent/deep-research-i-and-love-and-you]
    paths: [.github/workflows/reissue-ily-next20-multipacks.yml]
permissions:
  contents: write
jobs:
  reissue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/deep-research-i-and-love-and-you
          fetch-depth: 0
      - run: python .github/scripts/reissue-ily-next20-multipacks.py
      - run: |
          git config user.name "ingredients-research-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/deep-research-i-and-love-and-you.json
          git diff --cached --check
          git commit -m "research: reissue next ILY multipacks under contains contract"
          git push origin HEAD:agent/deep-research-i-and-love-and-you
# batch 3 trigger
